<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Background audio (radio) media notification -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"/>
    <!-- Install a downloaded update APK (app self-update) -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES"/>


    <!-- Declare touchscreen as not required for TV -->
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />
    
    <application
        android:label="FNDTV"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:banner="@drawable/banner"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
       >  
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
               
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!--
          Force the Skia GL backend instead of Impeller.

          The STB player embeds a real SurfaceView through a platform view in
          TRUE hybrid composition, which merges Flutter's raster and platform
          threads. On the Rockchip boxes that merge collides with Impeller's
          GLES backend: the raster thread cannot acquire its own EGL context and
          logs, continuously,
            eglMakeCurrentImpl:1100 error 3002 (EGL_BAD_ACCESS)
            impeller/toolkit/egl/egl.cc: EGL Error: Bad Access (12290)
            gpu_surface_gl_impeller.cc: Could not make the context current
          Flutter then renders NOTHING — video keeps showing because
          SurfaceFlinger composites the SurfaceView independently, but every
          control, spinner and overlay above it is frozen. Captured on the box
          2026-08-03 17:05.

          Skia's GL backend handles the merged-thread case that Impeller's does
          not here. These boxes are also the weakest GPUs we ship to (RK3328 is
          a GLES2-only Mali-450), which is the worst case for Impeller GLES
          anyway. Revisit only with a box in hand to test on.
        -->
        <meta-data
            android:name="io.flutter.embedding.android.EnableImpeller"
            android:value="false" />

        <!-- just_audio_background: media playback service + media-button receiver -->
        <service android:name="com.ryanheise.audioservice.AudioService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="true">
            <intent-filter>
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
        </service>
        <receiver android:name="com.ryanheise.audioservice.MediaButtonReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </receiver>

        <!-- FileProvider for handing the downloaded update APK to the installer -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>
    </application>
    
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Allow url_launcher to open https links (donation pages) on Android 11+ -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
    </queries>
</manifest>